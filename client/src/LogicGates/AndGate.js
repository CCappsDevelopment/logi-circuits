import React from 'react';
import LogicGate from './LogicGate';

const AndGate = (props) => {
    return (
        <LogicGate {...props} color="#11DDDA" text="AND" />
    );
}

export default AndGate;